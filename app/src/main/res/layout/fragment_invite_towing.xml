<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:fitsSystemWindows="true"
    tools:context=".fragments.InviteTowingFragment">


    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/invite_towing_map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/invite_towing_BTN_invite"

        />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/invite_towing_BTN_invite"
        style="@style/Widget.App.ButtonStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/orange"
        android:padding="20dp"
        android:text="@string/invite"
        android:textColor="@color/white" />

    <ProgressBar
        android:id="@+id/towing_detail_PRG"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_alignParentBottom="true"
        android:theme="@style/myProgressBar"
        android:visibility="invisible" />


</RelativeLayout>